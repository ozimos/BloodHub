# Migration `20191126103557-init`

This migration has been generated by Tovieye Moses Ozi at 11/26/2019, 10:35:57 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."BloodRequest" (
  "bloodGroup" text NOT NULL DEFAULT 'A_positive' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "status" text NOT NULL DEFAULT 'pending' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Donor" (
  "bloodGroup" text NOT NULL DEFAULT 'A_positive' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "document" text   ,
  "id" text NOT NULL  ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Hospital" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "lg" text NOT NULL DEFAULT '' ,
  "name" text NOT NULL DEFAULT '' ,
  "state" text NOT NULL DEFAULT '' ,
  "street" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."User" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "email" text NOT NULL DEFAULT '' ,
  "firstName" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "lastName" text   ,
  "lg" text   ,
  "password" text NOT NULL DEFAULT '' ,
  "phone" text NOT NULL DEFAULT '' ,
  "state" text   ,
  "street" text   ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."BloodRequest" ADD COLUMN "hospitalId" text   REFERENCES "public"."Hospital"("id") ON DELETE SET NULL,
ADD COLUMN "requesterId" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Donor" ADD COLUMN "userId" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191126103557-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,76 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DB_URL")
+  enabled = env("PRISMA_PROVIDER")
+}
+
+datasource test_db {
+  provider = "postgresql"
+  url      = env("TEST_DATABASE_URL")
+  enabled = env("PRISMA_TEST_PROVIDER")
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+enum BloodGroup {
+     A_positive
+        A_negative
+        B_positive
+        B_negative
+        AB_positive
+        AB_negative
+        O_positive
+        O_negative
+}
+
+enum Status {
+  pending
+  matched
+  reprocessing
+  failed
+  success
+}
+
+model BloodRequest {
+  id String @id @default(cuid())
+  requester User  @map("requesterId")
+  bloodGroup BloodGroup
+  hospital Hospital @map("hospitalId")
+  status Status
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+model Donor {
+  id String @id @default(cuid())
+  user User @map("userId") @relation(references: [id])
+  bloodGroup BloodGroup
+  document String?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+model Hospital {
+  id String @id @default(cuid())
+  name String
+  street String
+  lg String
+  state String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model User {
+  id String    @id @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  firstName     String
+  lastName   String?
+  email     String @unique
+  phone     String
+  street     String?
+  lg     String?
+  state     String?
+  password     String
+  donor Donor?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191126103557-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126103557-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
