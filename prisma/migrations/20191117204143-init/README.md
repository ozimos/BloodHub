# Migration `20191117204143-init`

This migration has been generated by Tovieye Moses Ozi at 11/17/2019, 8:41:43 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."BloodRequest" (
  "bloodGroup" text NOT NULL DEFAULT 'A_positive' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" SERIAL,
  "status" text NOT NULL DEFAULT 'pending' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Donor" (
  "bloodGroup" text NOT NULL DEFAULT 'A_positive' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "document" text   ,
  "id" SERIAL,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Hospital" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" SERIAL,
  "lg" text NOT NULL DEFAULT '' ,
  "name" text NOT NULL DEFAULT '' ,
  "state" text NOT NULL DEFAULT '' ,
  "street" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."User" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "email" text NOT NULL DEFAULT '' ,
  "firstName" text NOT NULL DEFAULT '' ,
  "id" SERIAL,
  "lastName" text   ,
  "lg" text   ,
  "password" text NOT NULL DEFAULT '' ,
  "phone" text NOT NULL DEFAULT '' ,
  "state" text   ,
  "street" text   ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."BloodRequest" ADD COLUMN "hospitalId" integer   REFERENCES "public"."Hospital"("id") ON DELETE SET NULL,
ADD COLUMN "requesterId" integer   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Donor" ADD COLUMN "userId" integer   REFERENCES "public"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191117204143-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,76 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+  enabled = env("PRISMA_PROVIDER")
+}
+
+datasource test_db {
+  provider = "postgresql"
+  url      = env("TEST_DATABASE_URL")
+  enabled = env("PRISMA_TEST_PROVIDER")
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+enum BloodGroup {
+     A_positive
+        A_negative
+        B_positive
+        B_negative
+        AB_positive
+        AB_negative
+        O_positive
+        O_negative
+}
+
+enum Status {
+  pending
+  matched
+  reprocessing
+  failed
+  success
+}
+
+model BloodRequest {
+  id Int @id
+  requester User  @map("requesterId")
+  bloodGroup BloodGroup
+  hospital Hospital @map("hospitalId")
+  status Status
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+model Donor {
+  id Int @id
+  user User @map("userId") @relation(references: [id])
+  bloodGroup BloodGroup
+  document String?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+model Hospital {
+  id Int @id
+  name String
+  street String
+  lg String
+  state String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model User {
+  id        Int    @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  firstName     String
+  lastName   String?
+  email     String
+  phone     String
+  street     String?
+  lg     String?
+  state     String?
+  password     String
+  donor Donor?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191117204143-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191117204143-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
